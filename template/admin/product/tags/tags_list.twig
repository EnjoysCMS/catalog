{% extends '@a/layout.twig' %}
{% block content %}

    {% do asset('js', [
        'modules/catalog/node_modules/bootstrap-tagsinput/dist/bootstrap-tagsinput.min.js'
    ]) %}
    {% do asset('css', [
        'modules/catalog/node_modules/bootstrap-tagsinput/dist/bootstrap-tagsinput.css'
    ]) %}

    <script>
        $('#tags').tagsinput({
            tagClass: 'badge bg-dark'
        });
    </script>
    <section class="content">


        <div class="card">
            <div class="card-header">
                <h2 class="card-title">{{ title ?? product.getName }}
                    <a class="small text-dark" href="{{ path('catalog/product', {"slug": product.getSlug}) }}"><i class="fas fa-external-link-alt"></i></a>
                    <small class="d-block">{{ subtitle }}</small>
                </h2>
                <div class="float-right">
                    {% include '../modules/catalog/template/admin/products_admin_links.twig' %}
                </div>
            </div>

            <!-- /.card-header -->
            <div class="card-body">
                {{ form|raw }}
            </div>
            <!-- /.card-body -->
        </div>
        <!-- /.card -->

    </section>

    {% block additional %}
    {% endblock %}

{% endblock %}