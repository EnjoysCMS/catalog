{% extends '@a/layout.twig' %}
{% block content %}
    {% do asset('js', ['modules/admin/node_modules/bs-custom-file-input/dist/bs-custom-file-input.min.js']) %}
    <script>
        $(document).ready(function () {
            bsCustomFileInput.init()
        })
    </script>

    <section class="content">


        <div class="card">

            <div class="card-header">
                <h2 class="card-title">Список брендов</h2>
            </div>
            <!-- /.card-header -->
            <div class="card-body">
                <ul>
                {% for brand in brands %}
                    <li>{{ brand.name }} {% if brand.logo %}<img style="height: 50px" src="{{ brand.logo }}" />{% endif %}</li>
                {% endfor %}
                </ul>
            </div>
            <!-- /.card-body -->
        </div>
        <!-- /.card -->

    </section>

    {% block additional %}
    {% endblock %}

{% endblock %}

{% block scripts %}
    {{ editorEmbedCode|raw }}
{% endblock %}
